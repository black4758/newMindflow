server:
  port: 8453
  address: localhost

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/mindflow_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&zeroDateTimeBehavior=convertToNull&rewriteBatchedStatements=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      username: REDACTED
      password: REDACTED

  data:
    mongodb:
      uri: mongodb://localhost:27017/mindflow_db

  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: REDACTED

python:
  server:
    url: http://127.0.0.1:5001